// Remember that this is the path to lib embedded in the GH-page folder
// and not the lib that is compiled by the build script.
import KeysKey from "../../lib/index.js";

export function isUppercaseLetters(event) {
    let result = false;

    result = KeysKey.Is(event, KeysKey.Letter.A);
    if (result) {
        result = false;
        document.getElementById("A").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.B);
    if (result) {
        result = false;
        document.getElementById("B").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.C);
    if (result) {
        result = false;
        document.getElementById("C").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.D);
    if (result) {
        result = false;
        document.getElementById("D").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.E);
    if (result) {
        result = false;
        document.getElementById("E").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.F);
    if (result) {
        result = false;
        document.getElementById("F").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.G);
    if (result) {
        result = false;
        document.getElementById("G").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.H);
    if (result) {
        result = false;
        document.getElementById("H").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.I);
    if (result) {
        result = false;
        document.getElementById("I").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.J);
    if (result) {
        result = false;
        document.getElementById("J").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.K);
    if (result) {
        result = false;
        document.getElementById("K").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.L);
    if (result) {
        result = false;
        document.getElementById("L").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.M);
    if (result) {
        result = false;
        document.getElementById("M").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.N);
    if (result) {
        result = false;
        document.getElementById("N").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.O);
    if (result) {
        result = false;
        document.getElementById("O").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.P);
    if (result) {
        result = false;
        document.getElementById("P").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.Q);
    if (result) {
        result = false;
        document.getElementById("Q").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.R);
    if (result) {
        result = false;
        document.getElementById("R").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.S);
    if (result) {
        result = false;
        document.getElementById("S").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.T);
    if (result) {
        result = false;
        document.getElementById("T").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.U);
    if (result) {
        result = false;
        document.getElementById("U").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.V);
    if (result) {
        result = false;
        document.getElementById("V").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.W);
    if (result) {
        result = false;
        document.getElementById("W").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.X);
    if (result) {
        result = false;
        document.getElementById("X").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.Y);
    if (result) {
        result = false;
        document.getElementById("Y").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.Z);
    if (result) {
        result = false;
        document.getElementById("Z").classList.add("grid-item-active");
    }
}

export function isLowercaseLetters(event) {
    let result = false;

    result = KeysKey.Is(event, KeysKey.Letter.a);
    if (result) {
        result = false;
        document.getElementById("a").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.b);
    if (result) {
        result = false;
        document.getElementById("b").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.c);
    if (result) {
        result = false;
        document.getElementById("c").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.d);
    if (result) {
        result = false;
        document.getElementById("d").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.e);
    if (result) {
        result = false;
        document.getElementById("e").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.f);
    if (result) {
        result = false;
        document.getElementById("f").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.g);
    if (result) {
        result = false;
        document.getElementById("g").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.h);
    if (result) {
        result = false;
        document.getElementById("h").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.i);
    if (result) {
        result = false;
        document.getElementById("i").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.j);
    if (result) {
        result = false;
        document.getElementById("j").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.k);
    if (result) {
        result = false;
        document.getElementById("k").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.l);
    if (result) {
        result = false;
        document.getElementById("l").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.m);
    if (result) {
        result = false;
        document.getElementById("m").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.n);
    if (result) {
        result = false;
        document.getElementById("n").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.o);
    if (result) {
        result = false;
        document.getElementById("o").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.p);
    if (result) {
        result = false;
        document.getElementById("p").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.q);
    if (result) {
        result = false;
        document.getElementById("q").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.r);
    if (result) {
        result = false;
        document.getElementById("r").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.s);
    if (result) {
        result = false;
        document.getElementById("s").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.t);
    if (result) {
        result = false;
        document.getElementById("t").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.u);
    if (result) {
        result = false;
        document.getElementById("u").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.v);
    if (result) {
        result = false;
        document.getElementById("v").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.w);
    if (result) {
        result = false;
        document.getElementById("w").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.x);
    if (result) {
        result = false;
        document.getElementById("x").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.y);
    if (result) {
        result = false;
        document.getElementById("y").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Letter.z);
    if (result) {
        result = false;
        document.getElementById("z").classList.add("grid-item-active");
    }
}

export function isNumbers(event) {
    let result = false;

    result = KeysKey.Is(event, KeysKey.Number.Zero);
    if (result) {
        result = false;
        document.getElementById("0").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.One);
    if (result) {
        result = false;
        document.getElementById("1").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Two);
    if (result) {
        result = false;
        document.getElementById("2").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Three);
    if (result) {
        result = false;
        document.getElementById("3").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Four);
    if (result) {
        result = false;
        document.getElementById("4").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Five);
    if (result) {
        result = false;
        document.getElementById("5").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Six);
    if (result) {
        result = false;
        document.getElementById("6").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Seven);
    if (result) {
        result = false;
        document.getElementById("7").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Eight);
    if (result) {
        result = false;
        document.getElementById("8").classList.add("grid-item-active");
    }

    result = KeysKey.Is(event, KeysKey.Number.Nine);
    if (result) {
        result = false;
        document.getElementById("9").classList.add("grid-item-active");
    }
}
